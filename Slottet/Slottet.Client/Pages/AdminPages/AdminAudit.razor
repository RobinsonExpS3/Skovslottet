@page "/adminAudit"
@rendermode InteractiveWebAssembly

<div class="container">
    <div class="top-bar">
        <div class="date-picker">
            <label for="audit-date">Dato</label>
            <input id="audit-date" type="date" min="2026-04-01" value="@SelectedDateText" @onchange="OnDateChanged"/>
        </div>
        
        <div>
            <label for="audit-shift">Vagt</label>
            <select id="audit-shift" class="shift-picker" value="@SelectedShift" @onchange="OnShiftChanged">
                <option value="">Vælg vagt</option>
                <option value="day">Dag</option>
                <option value="evening">Aften</option>
                <option value="night">Nat</option>
            </select>
        </div>
    </div>

    <div class="audit-panel">
        @if(IsLoading)
        {
            <p>Indlæser audit logs...</p>
        }
        else if(!string.IsNullOrWhiteSpace(ErrorMessage))
        {
            <p>@ErrorMessage</p>
        }
        else
        {
            <table class="audit-table">
                <thead>
                    <tr>
                        <th>Tid</th>
                        <th>Hvem</th>
                        <th>Handling</th>
                        <th>Tabel</th>
                        <th>Omhandler</th>
                        <th>Tidligere Værdi</th>
                        <th>Ny Værdi</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var row in AuditDisplayRows) {
                        <tr>
                            <td>@row.Time.ToString("dd-MM-yyyy HH:mm")</td>
                            <td>@row.PerformedBy</td>
                            <td>@row.Action</td>
                            <td>@row.TableName</td>
                            <td>@row.Subject</td>
                            <td>@row.OldValue</td>
                            <td>@row.NewValue</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>
